on: push
jobs:
  test:
    strategy:
      matrix:
        node: [ 10, 12, 14 ]
        os: [ ubuntu-latest, macos-latest ]
        exclude:
          - node: 10
            os: ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - run: echo ...

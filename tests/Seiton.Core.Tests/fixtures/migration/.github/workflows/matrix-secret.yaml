on: push
jobs:
  build:
    runs-on: ubuntu-latest
    permissions: {}
    timeout-minutes: 10
    steps:
      - env:
          A: ${{ secrets.A }}
          B: ${{ secrets.B }}
          C: ${{ secrets.C }}
          D: ${{ secrets.D }}
          E: ${{ secrets.E }}
          F: ${{ secrets.F }}
        run: echo matrix

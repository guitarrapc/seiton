verbose: config: D:\github\guitarrapc\githubactions-lab\.github\seiton-test.yaml (from --config)
verbose: discovery: searching under cwd D:\github\guitarrapc\githubactions-lab
verbose: discovery: found D:\github\guitarrapc\githubactions-lab\.github\workflows
verbose: discovery: 120 file(s) resolved
verbose: discovery: skipped D:\github\guitarrapc\githubactions-lab\.github\workflows\monthly-oss-repo-status.lock.yml (agentic workflow)
verbose: config: fix.pinning.enable-network=true (source: config)
verbose: config: fix.images.enable-network=true (source: config)
verbose: rules: 59 enabled, 1 disabled (workflow)
verbose: rules: disabled: concurrency-limits
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\_reusable-dump-context.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\_reusable-dump-context.yaml
@@ -74,6 +74,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          env | tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.sh"
+          mkdir -p "${OUTPUT_PATH}"
+          env | tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.sh"
         env:
           CONTEXT: ${{ toJson(env) }}
@@ -82,6 +82,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.json" < "$GITHUB_EVENT_PATH"
+          mkdir -p "${OUTPUT_PATH}"
+          tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.json" < "$GITHUB_EVENT_PATH"
         env:
           OUTPUT_PATH: ${{ inputs.output-path }}/github_event_path
@@ -89,6 +89,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          echo "$CONTEXT" | jq '.token="<reducted>"' | tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.json"
+          mkdir -p "${OUTPUT_PATH}"
+          echo "$CONTEXT" | jq '.token="<reducted>"' | tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.json"
         env:
           CONTEXT: ${{ toJson(github) }}
@@ -97,6 +97,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          echo "$CONTEXT" | tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.json"
+          mkdir -p "${OUTPUT_PATH}"
+          echo "$CONTEXT" | tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.json"
         env:
           CONTEXT: ${{ toJson(job) }}
@@ -105,6 +105,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          echo "$CONTEXT" | tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.json"
+          mkdir -p "${OUTPUT_PATH}"
+          echo "$CONTEXT" | tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.json"
         env:
           CONTEXT: ${{ toJson(steps) }}
@@ -113,6 +113,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          echo "$CONTEXT" | tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.json"
+          mkdir -p "${OUTPUT_PATH}"
+          echo "$CONTEXT" | tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.json"
         env:
           CONTEXT: ${{ toJson(runner) }}
@@ -121,6 +121,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          echo "$CONTEXT" | tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.json"
+          mkdir -p "${OUTPUT_PATH}"
+          echo "$CONTEXT" | tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.json"
         env:
           CONTEXT: ${{ toJson(strategy) }}
@@ -129,6 +129,6 @@
         shell: bash
         run: |
-          mkdir -p "${{ env.OUTPUT_PATH }}"
-          echo "$CONTEXT" | tee "${{ env.OUTPUT_PATH }}/${{ steps.file.outputs.name }}.json"
+          mkdir -p "${OUTPUT_PATH}"
+          echo "$CONTEXT" | tee "${OUTPUT_PATH}/${{ steps.file.outputs.name }}.json"
         env:
           CONTEXT: ${{ toJson(matrix) }}
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\_reusable-workflow-called.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\_reusable-workflow-called.yaml
@@ -63,5 +63,5 @@
           APPLES: ${{ secrets.APPLES }}
       - name: called env (global)
-        run: echo "called global env. ${{ env.FOO }}"
+        run: echo "called global env. ${FOO}"
       - name: output step1
         id: step1
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\create-release-simple.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\create-release-simple.yaml
@@ -15,5 +15,5 @@
       - name: Setup tag
         id: tag
-        run: echo "value=${{ env.TAG_VALUE }}" | tee -a "$GITHUB_OUTPUT"
+        run: echo "value=${TAG_VALUE}" | tee -a "$GITHUB_OUTPUT"
         env:
           TAG_VALUE: ${{ github.ref_name }}
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\create-release.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\create-release.yaml
@@ -34,5 +34,7 @@
       - name: Setup tag
         id: tag
-        run: echo "value=${{ inputs.tag || (github.event_name == 'pull_request' && '0.1.0-test' || github.ref_name) }}" | tee -a "$GITHUB_OUTPUT"
+        run: echo "value=${TAG}" | tee -a "$GITHUB_OUTPUT"
+        env:
+          TAG: ${{ inputs.tag || (github.event_name == 'pull_request' && '0.1.0-test' || github.ref_name) }}
       # Create Tag
       - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\create-tag.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\create-tag.yaml
@@ -21,5 +21,5 @@
           persist-credentials: false
       - name: tag
-        run: git tag ${{ env.TAG }}
+        run: git tag ${TAG}
       - name: Push changes
         uses: ad-m/github-push-action@4cc74773234f74829a8c21bc4d69dd4be9cfa599 # v1.1.0
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\default-shell.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\default-shell.yaml
@@ -26,9 +26,9 @@
         id: shell
         run: |
-          echo "BRANCH=${{ env.BRANCH_NAME }}" | tee -a "$GITHUB_ENV"
-          echo "branch=${{ env.BRANCH_NAME }}" | tee -a "$GITHUB_OUTPUT"
+          echo "BRANCH=${BRANCH_NAME}" | tee -a "$GITHUB_ENV"
+          echo "branch=${BRANCH_NAME}" | tee -a "$GITHUB_OUTPUT"
       - name: Show ENV and OUTPUT
         run: |
-          echo ${{ env.BRANCH }}
+          echo ${BRANCH}
           echo ${{ steps.shell.outputs.branch }}
       - name: Add PATH
@@ -55,9 +55,9 @@
         id: shell
         run: |
-          echo "BRANCH=${{ env.BRANCH_NAME }}" | Tee-Object -Append -FilePath "${env:GITHUB_ENV}"
-          echo "branch=${{ env.BRANCH_NAME }}" | Tee-Object -Append -FilePath "${env:GITHUB_OUTPUT}"
+          echo "BRANCH=$env:BRANCH_NAME" | Tee-Object -Append -FilePath "${env:GITHUB_ENV}"
+          echo "branch=$env:BRANCH_NAME" | Tee-Object -Append -FilePath "${env:GITHUB_OUTPUT}"
       - name: Show ENV and OUTPUT
         run: |
-          echo "${{ env.BRANCH }}"
+          echo "$env:BRANCH"
           echo "${{ steps.shell.outputs.branch }}"
       - name: Add PATH
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\fake-ternary.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\fake-ternary.yaml
@@ -23,5 +23,5 @@
     timeout-minutes: 3
     steps:
-      - run: echo "GROUP=[${{ env.GROUP }}]"
-      - run: echo "GROUP2=[${{ env.GROUP2 }}]"
-      - run: echo "VALUE=[${{ env.VALUE }}]"
+      - run: echo "GROUP=[${GROUP}]"
+      - run: echo "GROUP2=[${GROUP2}]"
+      - run: echo "VALUE=[${VALUE}]"
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\gitops-k8s-manifest.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\gitops-k8s-manifest.yaml
@@ -40,7 +40,7 @@
       - name: generate manifest
         run: |
-          mkdir -p "./${{ env.MANIFEST_REPO }}/${{ env.MANIFEST_DIR }}"
+          mkdir -p "./${MANIFEST_REPO}/${MANIFEST_DIR}"
           kubectl kustomize ./src/k8s/app/overlays/development \
-            | sed -e "s|<namespace>|${NAMESPACE_PREFIX}${{ env.MANIFEST_DIR }}|g" \
+            | sed -e "s|<namespace>|${NAMESPACE_PREFIX}${MANIFEST_DIR}|g" \
             | sed -e "s|<git-sha>|${{ github.sha }}|g" \
             | sed -e "s|<git-branch>|${BRANCH}|g" \
@@ -47,5 +47,5 @@
             | sed -e "s|<git-link>|${GITHUB_PR_URL}|g" \
             | sed -e "s|<build-id>|${{ github.run_id }}|g" \
-            | tee "${{ env.MANIFEST_REPO }}/${{ env.MANIFEST_DIR }}/all.yaml"
+            | tee "${MANIFEST_REPO}/${MANIFEST_DIR}/all.yaml"
         env:
           BRANCH: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref_name }}
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\matrix-secret.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\matrix-secret.yaml
@@ -31,5 +31,5 @@
         env:
           SECRET: ${{ secrets[matrix.secret] }} # zizmor: ignore[overprovisioned-secrets]
-      - run: echo "env:${{ env.fruit }} secret:${SECRET}"
+      - run: echo "env:${fruit} secret:${SECRET}"
         env:
           SECRET: ${{ secrets[env.fruit] }} # zizmor: ignore[overprovisioned-secrets]
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\setenv-script.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\setenv-script.yaml
@@ -28,8 +28,8 @@
       - name: Add ENV and OUTPUT by Script
         id: script
-        run: bash ./.github/scripts/setenv.sh --ref "${{ env.BRANCH_NAME }}"
+        run: bash ./.github/scripts/setenv.sh --ref "${BRANCH_NAME}"
       - name: Show Script  ENV and OUTPUT
         run: |
-          echo ${{ env.BRANCH_SCRIPT }}
+          echo ${BRANCH_SCRIPT}
           echo ${{ steps.script.outputs.branch }}
 
@@ -51,7 +51,7 @@
       - name: Add ENV and OUTPUT by Script
         id: script
-        run: ./.github/scripts/setenv.ps1 -Ref "${{ env.BRANCH_NAME }}"
+        run: ./.github/scripts/setenv.ps1 -Ref "$env:BRANCH_NAME"
       - name: Show Script ENV and OUTPUT
         run: |
-          echo "${{ env.BRANCH_SCRIPT }}"
+          echo "$env:BRANCH_SCRIPT"
           echo "${{ steps.script.outputs.branch }}"
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\setup-dotnet.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\setup-dotnet.yaml
@@ -48,6 +48,6 @@
           echo "::endgroup::"
 
-          echo "::group::dotnet files under ${{ env.DOTNET_ROOT }}/sdk"
-            ls -l "${{ env.DOTNET_ROOT }}/sdk"
+          echo "::group::dotnet files under ${DOTNET_ROOT}/sdk"
+            ls -l "${DOTNET_ROOT}/sdk"
           echo "::endgroup::"
 
@@ -85,6 +85,6 @@
           echo "::endgroup::"
 
-          echo "::group::dotnet files under ${{ env.DOTNET_ROOT }}/sdk"
-          ls -l "${{ env.DOTNET_ROOT }}/sdk"
+          echo "::group::dotnet files under ${DOTNET_ROOT}/sdk"
+          ls -l "${DOTNET_ROOT}/sdk"
           echo "::endgroup::"
 
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\tag-push-only-context.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\tag-push-only-context.yaml
@@ -20,5 +20,5 @@
         run: |
           echo "${{ steps.CI_TAG.outputs.GIT_TAG }}"
-          echo "${{ env.GIT_TAG }}"
+          echo "${GIT_TAG}"
       - name: Show tag value by github.ref_name
         run: echo "${GITHUB_REF_NAME}"
--- D:\github\guitarrapc\githubactions-lab\.github\workflows\workflowdispatch-inputs.yaml
+++ D:\github\guitarrapc\githubactions-lab\.github\workflows\workflowdispatch-inputs.yaml
@@ -45,5 +45,5 @@
       - name: Show Environment Variables
         run: env
-      - run: echo "BRANCH=${{ env.BRANCH }}, LOGLEVEL=${{ env.LOGLEVEL }}, TAGS=${{ env.TAGS }}, DRY_RUN=${{ env.DRY_RUN }}"
+      - run: echo "BRANCH=${BRANCH}, LOGLEVEL=${LOGLEVEL}, TAGS=${TAGS}, DRY_RUN=${DRY_RUN}"
       - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
         with:
verbose: suppressed: 10 diagnostic(s) (dangerous-triggers: 2, deny-inherit-secrets: 2, env-var: 2, if-cond: 2, unpinned-uses: 2)
verbose: excluded: 2 file(s): D:\github\guitarrapc\githubactions-lab\.github\workflows\agentics-maintenance.yml, D:\github\guitarrapc\githubactions-lab\.github\workflows\injection-attack-via-context.yaml
Would fix 48 of 53 issues in 14 files (5 remaining)

| File                           | Would Fix | Remaining |
|--------------------------------|----------:|----------:|
| _reusable-dump-context.yaml    |        16 |         0 |
| default-shell.yaml             |         6 |         0 |
| gitops-k8s-manifest.yaml       |         5 |         0 |
| matrix-secret.yaml             |         1 |         3 |
| setenv-script.yaml             |         4 |         0 |
| setup-dotnet.yaml              |         4 |         0 |
| workflowdispatch-inputs.yaml   |         4 |         0 |
| fake-ternary.yaml              |         3 |         0 |
| _reusable-workflow-called.yaml |         1 |         1 |
| create-release-simple.yaml     |         1 |         0 |
| create-release.yaml            |         1 |         0 |
| create-tag.yaml                |         1 |         0 |
| secrets-access.yaml            |         0 |         1 |
| tag-push-only-context.yaml     |         1 |         0 |

| Rule                          | Would Fix |
|-------------------------------|----------:|
| run-env-context-direct-use    |        47 |
| run-inputs-context-direct-use |         1 |
1 error, 4 warnings remain in 3 files (2 excluded, 10 suppressed)

| Rule                           | Remaining |
|--------------------------------|----------:|
| unredacted-secrets             |         3 |
| env-var                        |         1 |
| run-secrets-context-direct-use |         1 |

warning[unredacted-secrets]: run script may print secret-derived variable 'APPLES' without masking; avoid echo/printf/Write-Host of secret values
  --> .github/workflows/_reusable-workflow-called.yaml:61:35
   |

warning[env-var]: workflow.env key 'fruit' is not portable; use [A-Z_][A-Z0-9_]* naming
  --> .github/workflows/matrix-secret.yaml:10:3
   |

warning[unredacted-secrets]: run script may print secret-derived variable 'SECRET' without masking; avoid echo/printf/Write-Host of secret values
  --> .github/workflows/matrix-secret.yaml:30:49
   |

warning[unredacted-secrets]: run script may print secret-derived variable 'SECRET' without masking; avoid echo/printf/Write-Host of secret values
  --> .github/workflows/matrix-secret.yaml:33:40
   |

error[run-secrets-context-direct-use]: run script must not reference ${{ secrets.* }} directly; map secrets to env and use shell variables instead (e.g. ${TOKEN}, $TOKEN, or $env:TOKEN)
  --> .github/workflows/secrets-access.yaml:16:26
   |

verbose: total: 119 file(s) processed, 13 would be modified in 68.2 ms
